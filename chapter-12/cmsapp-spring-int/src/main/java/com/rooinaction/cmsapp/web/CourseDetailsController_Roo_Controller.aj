// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.rooinaction.cmsapp.web;

import com.rooinaction.cmsapp.domain.CourseDetails;
import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.String;
import java.util.Collection;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect CourseDetailsController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST)
    public String CourseDetailsController.create(@Valid CourseDetails courseDetails, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("courseDetails", courseDetails);
            return "coursedetailses/create";
        }
        uiModel.asMap().clear();
        courseDetails.persist();
        return "redirect:/coursedetailses/" + encodeUrlPathSegment(courseDetails.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String CourseDetailsController.createForm(Model uiModel) {
        uiModel.addAttribute("courseDetails", new CourseDetails());
        return "coursedetailses/create";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    public String CourseDetailsController.show(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("coursedetails", CourseDetails.findCourseDetails(id));
        uiModel.addAttribute("itemId", id);
        return "coursedetailses/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String CourseDetailsController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("coursedetailses", CourseDetails.findCourseDetailsEntries(firstResult, sizeNo));
            float nrOfPages = (float) CourseDetails.countCourseDetailses() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("coursedetailses", CourseDetails.findAllCourseDetailses());
        }
        return "coursedetailses/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String CourseDetailsController.update(@Valid CourseDetails courseDetails, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("courseDetails", courseDetails);
            return "coursedetailses/update";
        }
        uiModel.asMap().clear();
        courseDetails.merge();
        return "redirect:/coursedetailses/" + encodeUrlPathSegment(courseDetails.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", method = RequestMethod.GET)
    public String CourseDetailsController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("courseDetails", CourseDetails.findCourseDetails(id));
        return "coursedetailses/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE)
    public String CourseDetailsController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        CourseDetails courseDetails = CourseDetails.findCourseDetails(id);
        courseDetails.remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/coursedetailses";
    }
    
    @ModelAttribute("coursedetailses")
    public Collection<CourseDetails> CourseDetailsController.populateCourseDetailses() {
        return CourseDetails.findAllCourseDetailses();
    }
    
    String CourseDetailsController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
