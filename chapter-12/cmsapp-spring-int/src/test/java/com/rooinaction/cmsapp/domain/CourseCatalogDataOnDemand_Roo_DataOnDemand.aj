// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.rooinaction.cmsapp.domain;

import com.rooinaction.cmsapp.domain.CourseCatalog;
import java.lang.String;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect CourseCatalogDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CourseCatalogDataOnDemand: @Component;
    
    private Random CourseCatalogDataOnDemand.rnd = new SecureRandom();
    
    private List<CourseCatalog> CourseCatalogDataOnDemand.data;
    
    public CourseCatalog CourseCatalogDataOnDemand.getNewTransientCourseCatalog(int index) {
        CourseCatalog obj = new CourseCatalog();
        setName(obj, index);
        return obj;
    }
    
    public void CourseCatalogDataOnDemand.setName(CourseCatalog obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public CourseCatalog CourseCatalogDataOnDemand.getSpecificCourseCatalog(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        CourseCatalog obj = data.get(index);
        java.lang.Long id = obj.getId();
        return CourseCatalog.findCourseCatalog(id);
    }
    
    public CourseCatalog CourseCatalogDataOnDemand.getRandomCourseCatalog() {
        init();
        CourseCatalog obj = data.get(rnd.nextInt(data.size()));
        java.lang.Long id = obj.getId();
        return CourseCatalog.findCourseCatalog(id);
    }
    
    public boolean CourseCatalogDataOnDemand.modifyCourseCatalog(CourseCatalog obj) {
        return false;
    }
    
    public void CourseCatalogDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CourseCatalog.findCourseCatalogEntries(from, to);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'CourseCatalog' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<com.rooinaction.cmsapp.domain.CourseCatalog>();
        for (int i = 0; i < 10; i++) {
            CourseCatalog obj = getNewTransientCourseCatalog(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> it = e.getConstraintViolations().iterator(); it.hasNext();) {
                    ConstraintViolation<?> cv = it.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
