// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.

package org.rooinaction.coursemanager.client.managed.activity;
import com.google.gwt.activity.shared.Activity;
import com.google.gwt.place.shared.PlaceController;
import com.google.web.bindery.requestfactory.shared.EntityProxyId;
import com.google.web.bindery.requestfactory.shared.RequestContext;
import org.rooinaction.coursemanager.client.managed.request.ApplicationRequestFactory;
import org.rooinaction.coursemanager.client.managed.ui.desktop.StudentDesktopDetailsView;
import org.rooinaction.coursemanager.client.managed.ui.desktop.StudentDesktopEditView;
import org.rooinaction.coursemanager.client.managed.ui.desktop.StudentDesktopListView;
import org.rooinaction.coursemanager.client.managed.ui.mobile.StudentMobileDetailsView;
import org.rooinaction.coursemanager.client.managed.ui.mobile.StudentMobileEditView;
import org.rooinaction.coursemanager.client.scaffold.ScaffoldApp;
import org.rooinaction.coursemanager.client.scaffold.place.ProxyPlace;
import org.rooinaction.coursemanager.web.proxy.StudentProxy;
import org.rooinaction.coursemanager.web.request.StudentRequest;

public abstract class StudentActivitiesMapper_Roo_Gwt {

    protected ApplicationRequestFactory factory;

    protected PlaceController placeController;

    protected Activity makeCreateActivity() {
        StudentDesktopEditView.instance().setCreating(true);
        Activity activity = new StudentEditActivity(null, factory, ScaffoldApp.isMobile() ? StudentMobileEditView.instance() : StudentDesktopEditView.instance(), placeController);
        return new StudentEditActivityWrapper(factory, ScaffoldApp.isMobile() ? StudentMobileEditView.instance() : StudentDesktopEditView.instance(), activity, null);
    }

    @SuppressWarnings("unchecked")
    protected EntityProxyId<StudentProxy> coerceId(ProxyPlace place) {
        return (EntityProxyId<StudentProxy>) place.getProxyId();
    }

    protected Activity makeEditActivity(ProxyPlace place) {
        StudentDesktopEditView.instance().setCreating(false);
        EntityProxyId<StudentProxy> proxyId = coerceId(place);
        Activity activity = new StudentEditActivity(proxyId, factory, ScaffoldApp.isMobile() ? StudentMobileEditView.instance() : StudentDesktopEditView.instance(), placeController);
        return new StudentEditActivityWrapper(factory, ScaffoldApp.isMobile() ? StudentMobileEditView.instance() : StudentDesktopEditView.instance(), activity, proxyId);
    }
}
