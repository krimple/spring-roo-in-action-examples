// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.

package org.rooinaction.coursemanager.client.managed.activity;

import com.google.gwt.activity.shared.Activity;
import com.google.gwt.place.shared.PlaceController;
import com.google.web.bindery.requestfactory.shared.EntityProxyId;
import com.google.web.bindery.requestfactory.shared.RequestContext;
import org.rooinaction.coursemanager.client.managed.request.ApplicationRequestFactory;
import org.rooinaction.coursemanager.client.managed.ui.TagDetailsView;
import org.rooinaction.coursemanager.client.managed.ui.TagEditView;
import org.rooinaction.coursemanager.client.managed.ui.TagListView;
import org.rooinaction.coursemanager.client.managed.ui.TagMobileDetailsView;
import org.rooinaction.coursemanager.client.managed.ui.TagMobileEditView;
import org.rooinaction.coursemanager.client.scaffold.ScaffoldApp;
import org.rooinaction.coursemanager.client.scaffold.place.CreateAndEditProxy;
import org.rooinaction.coursemanager.client.scaffold.place.FindAndEditProxy;
import org.rooinaction.coursemanager.client.scaffold.place.ProxyPlace;
import org.rooinaction.coursemanager.proxy.TagProxy;
import org.rooinaction.coursemanager.request.TagRequest;

public abstract class TagActivitiesMapper_Roo_Gwt {

    protected ApplicationRequestFactory requests;

    protected PlaceController placeController;

    protected Activity makeCreateActivity() {
        TagEditView.instance().setCreating(true);
        final TagRequest request = requests.tagRequest();
        Activity activity = new CreateAndEditProxy<TagProxy>(TagProxy.class, request, ScaffoldApp.isMobile() ? TagMobileEditView.instance() : TagEditView.instance(), placeController) {

            @Override
            protected RequestContext createSaveRequest(TagProxy proxy) {
                request.persist().using(proxy);
                return request;
            }
        };
        return new TagEditActivityWrapper(requests, ScaffoldApp.isMobile() ? TagMobileEditView.instance() : TagEditView.instance(), activity, null);
    }

    @SuppressWarnings("unchecked")
    protected EntityProxyId<org.rooinaction.coursemanager.proxy.TagProxy> coerceId(ProxyPlace place) {
        return (EntityProxyId<TagProxy>) place.getProxyId();
    }

    protected Activity makeEditActivity(ProxyPlace place) {
        TagEditView.instance().setCreating(false);
        EntityProxyId<TagProxy> proxyId = coerceId(place);
        Activity activity = new FindAndEditProxy<TagProxy>(proxyId, requests, ScaffoldApp.isMobile() ? TagMobileEditView.instance() : TagEditView.instance(), placeController) {

            @Override
            protected RequestContext createSaveRequest(TagProxy proxy) {
                TagRequest request = requests.tagRequest();
                request.persist().using(proxy);
                return request;
            }
        };
        return new TagEditActivityWrapper(requests, ScaffoldApp.isMobile() ? TagMobileEditView.instance() : TagEditView.instance(), activity, proxyId);
    }
}
