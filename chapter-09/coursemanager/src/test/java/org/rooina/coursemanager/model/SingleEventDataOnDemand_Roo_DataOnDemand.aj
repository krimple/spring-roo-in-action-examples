// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.rooina.coursemanager.model;

import java.util.List;
import java.util.Random;
import org.rooina.coursemanager.model.CourseDataOnDemand;
import org.rooina.coursemanager.model.SingleEvent;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect SingleEventDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SingleEventDataOnDemand: @Component;
    
    private Random SingleEventDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<SingleEvent> SingleEventDataOnDemand.data;
    
    @Autowired
    private CourseDataOnDemand SingleEventDataOnDemand.courseDataOnDemand;
    
    public SingleEvent SingleEventDataOnDemand.getNewTransientSingleEvent(int index) {
        org.rooina.coursemanager.model.SingleEvent obj = new org.rooina.coursemanager.model.SingleEvent();
        setCourse(obj, index);
        setLocation(obj, index);
        setRunDate(obj, index);
        return obj;
    }
    
    public void SingleEventDataOnDemand.setCourse(SingleEvent obj, int index) {
        org.rooina.coursemanager.model.Course course = courseDataOnDemand.getRandomCourse();
        obj.setCourse(course);
    }
    
    public void SingleEventDataOnDemand.setLocation(SingleEvent obj, int index) {
        java.lang.String location = "location_" + index;
        obj.setLocation(location);
    }
    
    public void SingleEventDataOnDemand.setRunDate(SingleEvent obj, int index) {
        java.util.Date runDate = new java.util.GregorianCalendar(java.util.Calendar.getInstance().get(java.util.Calendar.YEAR), java.util.Calendar.getInstance().get(java.util.Calendar.MONTH), java.util.Calendar.getInstance().get(java.util.Calendar.DAY_OF_MONTH), java.util.Calendar.getInstance().get(java.util.Calendar.HOUR_OF_DAY), java.util.Calendar.getInstance().get(java.util.Calendar.MINUTE), java.util.Calendar.getInstance().get(java.util.Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setRunDate(runDate);
    }
    
    public SingleEvent SingleEventDataOnDemand.getSpecificSingleEvent(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        SingleEvent obj = data.get(index);
        return SingleEvent.findSingleEvent(obj.getId());
    }
    
    public SingleEvent SingleEventDataOnDemand.getRandomSingleEvent() {
        init();
        SingleEvent obj = data.get(rnd.nextInt(data.size()));
        return SingleEvent.findSingleEvent(obj.getId());
    }
    
    public boolean SingleEventDataOnDemand.modifySingleEvent(SingleEvent obj) {
        return false;
    }
    
    public void SingleEventDataOnDemand.init() {
        data = org.rooina.coursemanager.model.SingleEvent.findSingleEventEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'SingleEvent' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<org.rooina.coursemanager.model.SingleEvent>();
        for (int i = 0; i < 10; i++) {
            org.rooina.coursemanager.model.SingleEvent obj = getNewTransientSingleEvent(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
