// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.rooinaction.coursemanager.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.rooinaction.coursemanager.domain.CourseDetail;
import org.rooinaction.coursemanager.domain.CourseDetailDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect CourseDetailDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CourseDetailDataOnDemand: @Component;
    
    private Random CourseDetailDataOnDemand.rnd = new SecureRandom();
    
    private List<CourseDetail> CourseDetailDataOnDemand.data;
    
    public CourseDetail CourseDetailDataOnDemand.getNewTransientCourseDetail(int index) {
        CourseDetail obj = new CourseDetail();
        setName(obj, index);
        return obj;
    }
    
    public void CourseDetailDataOnDemand.setName(CourseDetail obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public CourseDetail CourseDetailDataOnDemand.getSpecificCourseDetail(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CourseDetail obj = data.get(index);
        Long id = obj.getId();
        return CourseDetail.findCourseDetail(id);
    }
    
    public CourseDetail CourseDetailDataOnDemand.getRandomCourseDetail() {
        init();
        CourseDetail obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CourseDetail.findCourseDetail(id);
    }
    
    public boolean CourseDetailDataOnDemand.modifyCourseDetail(CourseDetail obj) {
        return false;
    }
    
    public void CourseDetailDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CourseDetail.findCourseDetailEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CourseDetail' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CourseDetail>();
        for (int i = 0; i < 10; i++) {
            CourseDetail obj = getNewTransientCourseDetail(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
