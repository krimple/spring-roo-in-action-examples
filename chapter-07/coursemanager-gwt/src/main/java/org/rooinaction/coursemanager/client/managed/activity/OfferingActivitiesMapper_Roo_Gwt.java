// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.

package org.rooinaction.coursemanager.client.managed.activity;

import com.google.gwt.activity.shared.Activity;
import com.google.gwt.place.shared.PlaceController;
import com.google.web.bindery.requestfactory.shared.EntityProxyId;
import com.google.web.bindery.requestfactory.shared.RequestContext;
import java.util.Set;
import org.rooinaction.coursemanager.client.managed.request.ApplicationRequestFactory;
import org.rooinaction.coursemanager.client.managed.ui.OfferingDetailsView;
import org.rooinaction.coursemanager.client.managed.ui.OfferingEditView;
import org.rooinaction.coursemanager.client.managed.ui.OfferingListView;
import org.rooinaction.coursemanager.client.managed.ui.OfferingMobileDetailsView;
import org.rooinaction.coursemanager.client.managed.ui.OfferingMobileEditView;
import org.rooinaction.coursemanager.client.managed.ui.RegistrationSetEditor;
import org.rooinaction.coursemanager.client.scaffold.ScaffoldApp;
import org.rooinaction.coursemanager.client.scaffold.place.CreateAndEditProxy;
import org.rooinaction.coursemanager.client.scaffold.place.FindAndEditProxy;
import org.rooinaction.coursemanager.client.scaffold.place.ProxyPlace;
import org.rooinaction.coursemanager.web.gwt.proxies.CourseProxy;
import org.rooinaction.coursemanager.web.gwt.proxies.InstructorProxy;
import org.rooinaction.coursemanager.web.gwt.proxies.OfferingProxy;
import org.rooinaction.coursemanager.web.gwt.proxies.RegistrationProxy;
import org.rooinaction.coursemanager.web.gwt.requests.OfferingRequest;

public abstract class OfferingActivitiesMapper_Roo_Gwt {

    protected ApplicationRequestFactory requests;

    protected PlaceController placeController;

    protected Activity makeCreateActivity() {
        OfferingEditView.instance().setCreating(true);
        final OfferingRequest request = requests.offeringRequest();
        Activity activity = new CreateAndEditProxy<OfferingProxy>(OfferingProxy.class, request, ScaffoldApp.isMobile() ? OfferingMobileEditView.instance() : OfferingEditView.instance(), placeController) {

            @Override
            protected RequestContext createSaveRequest(OfferingProxy proxy) {
                request.persist().using(proxy);
                return request;
            }
        };
        return new OfferingEditActivityWrapper(requests, ScaffoldApp.isMobile() ? OfferingMobileEditView.instance() : OfferingEditView.instance(), activity, null);
    }

    @SuppressWarnings("unchecked")
    protected EntityProxyId<org.rooinaction.coursemanager.web.gwt.proxies.OfferingProxy> coerceId(ProxyPlace place) {
        return (EntityProxyId<OfferingProxy>) place.getProxyId();
    }

    protected Activity makeEditActivity(ProxyPlace place) {
        OfferingEditView.instance().setCreating(false);
        EntityProxyId<OfferingProxy> proxyId = coerceId(place);
        Activity activity = new FindAndEditProxy<OfferingProxy>(proxyId, requests, ScaffoldApp.isMobile() ? OfferingMobileEditView.instance() : OfferingEditView.instance(), placeController) {

            @Override
            protected RequestContext createSaveRequest(OfferingProxy proxy) {
                OfferingRequest request = requests.offeringRequest();
                request.persist().using(proxy);
                return request;
            }
        };
        return new OfferingEditActivityWrapper(requests, ScaffoldApp.isMobile() ? OfferingMobileEditView.instance() : OfferingEditView.instance(), activity, proxyId);
    }
}
